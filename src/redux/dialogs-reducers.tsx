import React from 'react';
import {ActionTypes, StoreType} from "./state";

export const dialogsReducer = (store:StoreType,action:ActionTypes) => {
    return (
        <div>

        </div>
    );
};

