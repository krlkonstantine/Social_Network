import {ActionType, SidebarPropsType} from "../store";

const initialState: SidebarPropsType = {}

export const sidebarReducer = (state: SidebarPropsType = initialState, action: ActionType) => {
    return state
}